import styled from 'styled-components';

export const NewsContainer = styled.div`
    display: flex;
    padding: 32px 24px;
    flex-direction: column;
    align-items: flex-end;
    gap: 32px;
    border-radius: 16px;
    border: 1px solid var(--foundation-grey-grey-700, #1E1E20);
    background: rgba(12, 13, 14, 0.80);
`